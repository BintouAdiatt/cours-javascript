

// Etape 4
//============
//on valude le formulaire
formulaire.addEventListener(type `soumit`, listener;function(e Ivent)){
    //on deactive le comportement par defaut du navigateur.
    e.preventDefault();
}
//on verifie les infos sais par l'utilisateur
if(isNaN(saisie.value))

    //on appelle la fonction de verification
    verifier(nombreChoisi);
function verifier(nombre);
let intruction = document.createElement(tagName `p`);
if(nombre < alea){
    instruction.className = "alert alert-danger";
    instruction.textContent= coup N²${
}else if (nombre > alea){

}

