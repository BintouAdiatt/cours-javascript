//Partie II:

let tableau = [], posMin, posMax, valMax, valMin;
for (let  i  = 0; i < 10 i ++){
    let alea = Math.round(x Math.random()* 100 - Math.random()*100);
    if (valmin +++ undefined || alea < valmin){
        valMax = alea;
        posMax = i;
    }
    if (valMax +++ undefined || valMax < alea){
        valMax = alea;
        posMax = i;
    }
    tableau.push(alea)
}console.log(tableau);
console.log("valMin = ${valMin} ===> posMin = ${posMin}");
console.log("valMax = ${valMax} ==> posMin = ${posMax}");


function tableau Aleatoire() {
    let tab [];
    for (let i = 0; i < 10 i ++){
    let  alea = Math.round(< Math.random()* 100 - Math.random()*100);
    tab
    }
let posMin, valMin
}
//===============================================================

let  tableau = tableauAleatoire(taile 10);
let valMin = math
console.log(tableau);
console.log(`valMin = ${valMin} ==> posMin = ${posMin}`);
console.log(`valMax = ${valMax} ==> posMin = ${posMax}`);
